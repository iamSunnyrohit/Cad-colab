{
  "name": "cad-collab",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace=@cad-collab/server",
    "dev:client": "npm run dev --workspace=@cad-collab/client",
    "build:shared": "npm run build --workspace=@cad-collab/shared",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
